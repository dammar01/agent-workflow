# v3.1.1 OpenCode Config — FINAL Setup Prompt

> Paste prompt ini ke agent (Claude Code / opencode itu sendiri).
> Agent generate file persis seperti spec di bawah. Tidak boleh interpretasi ulang.
> Prompt ini untuk first setup / one-shot full install v3.1.1, bukan untuk migrasi incremental dari v3.1.0.

Gunakan `prompt/UPDATE_CONFIG_PROMPT.md` jika kondisi awal sudah berada pada global config v3.1.0 dan hanya perlu migrasi ke v3.1.1.

---

## DESIGN NOTES (v3.1.1 vs v3.1.0 FINAL)

**Delta v3.1.1:**

- add project-local `.workflow` workspace
- add `/.init`, `/.doctor`, `/.sweep`
- add project-local prompt handoff
- add session-bound state reset
- auto sweep after `/.execute -y`
- audit deferred / disabled
- no `/.repair` command
- resolver checks `.workflow/config.json.runtime.agent_workflow_path` before `AGENT_PATH`

**Session rule clarified:**

- main agent still owns `MAIN_SESSION_ID`
- workflow runtime must receive same id through `--session`
- one main-agent session equals one workflow-agent session
- new main-agent session means new workflow-agent session
- `.workflow/state.json` is snapshot only, not reuse authority
- do not create `.workflow/sessions/current.json`

**Mode rule clarified:**

- input starting with `/.` = workflow mode
- plain text input = chat mode

---

## DESIGN NOTES (v3.1.0 FINAL vs v3.1.0 draft & v3.0.1)

**Base structure:** v3.1.0 modular skeleton (AGENTS.md tipis + `skills/` + `reference/`).

**Behavioral parity dengan v3.0.1 (empirically validated):**

- Session isolation (`MAIN_SESSION_ID` + project root) — KEEP (empirical: cegah cache contamination antar project).
- "WAJIB output hasil" safeguard — KEEP (empirical: fix bug "model diam tanpa hasil").
- Fallback mode eksplisit — KEEP (resiliency saat AGENT_PATH unavailable).
- Setup prereqs Step 0A–0C — KEEP utuh (frequent fresh setup).
- Per-command skill files (explore/plan/execute/verify/audit/analyze) — KEEP (detail per-flow).
- PowerShell-first invocation — KEEP (Windows native).

**Cut dari v3.0.1 (no empirical justification):**

- Contract awareness pre-execute (deep downstream contract audit) — REMOVED.
- Caveman hard caps per-section table — REMOVED (hard rules tetap).

**Softened dari v3.0.1:**

- Cross-model audit — preference cross-model tapi flexible (kadang sama OK).
- Token budget threshold — keep di `reference/token-budget.md`, tidak prominent di AGENTS.md.
- Caveman sub-skills (`/.commit /.review /.compress`) — shallow detail (less-used).
- NL Map — ringkas (slash-dominant workflow).

**Structural improvement dari v3.1.0 draft:**

- Critical safeguards (session isolation, "WAJIB output", fallback) di-INLINE ke AGENTS.md supaya selalu load, bukan hanya di skill files.
- Setup prereqs dipisah ke `install_guide.md` (load on-demand, bukan setiap session).
- Per-command skill files di-restore (v3.1.0 draft hilangkan ini ke `workflow.md` tunggal).
- Command ownership dipertegas: `/.execute -y`, `/.verify`, `/.verify-quick` lokal; `/.audit` wajib `AGENT_PATH`.
- Lightweight risk classifier ditambahkan hanya untuk memilih `/.verify` vs `/.verify-quick`, bukan full contract awareness.
- Setup verification dipisah dari runtime smoke test; runtime precondition missing tidak otomatis menggagalkan setup file.
- `/.plan` final answer dipertegas milik OpenCode agent utama: workflow agent hanya evidence/reasoning material, OpenCode wajib synthesize structured plan + evidence-derived open questions.
- `/.analyze` final framing dipertegas milik OpenCode agent utama; workflow agent tetap primary evidence worker.
- `/.explore` final answer dipertegas milik OpenCode agent utama: workflow agent hanya evidence eksplorasi, OpenCode wajib synthesize hasil sesuai hint user.

---

## INSTRUCTION TO AGENT

Buat struktur berikut di `~/.config/opencode/`:

```
~/.config/opencode/
├─ AGENTS.md
├─ install_guide.md
├─ skills/
│  ├─ caveman.md
│  ├─ workflow.md
│  ├─ agent-workflow.md
│  ├─ graphify.md
│  ├─ safety.md
│  ├─ context7.md
│  ├─ memory.md
│  ├─ init.md
│  ├─ doctor.md
│  ├─ sweep.md
│  ├─ explore.md
│  ├─ plan.md
│  ├─ execute.md
│  ├─ verify.md
│  ├─ verify-quick.md
│  ├─ refactor.md
│  ├─ analyze.md
│  ├─ audit.md
│  └─ help.md
├─ commands/
│  ├─ init.md
│  ├─ doctor.md
│  ├─ sweep.md
│  ├─ explore.md
│  ├─ plan.md
│  ├─ execute.md
│  ├─ verify.md
│  ├─ verify-quick.md
│  ├─ refactor.md
│  ├─ analyze.md
│  ├─ audit.md
│  ├─ memory.md
│  ├─ help.md
│  ├─ commit.md
│  ├─ review.md
│  └─ compress.md
├─ reference/
│  ├─ invocation-examples.md
│  ├─ json-contract.md
│  ├─ token-budget.md
│  ├─ graphify-missing-protocol.md
│  └─ errors.md
└─ memory/
   ├─ PERSONAL_MEMORY.md
   ├─ DOMAIN_MAP.md
   └─ MEMORY.md
```

**Rule eksekusi:**

1. Tulis konten file **persis** seperti block di antara `===== FILE: path =====` dan `===== END FILE =====`.
2. Buat folder kalau belum ada.
3. Kalau file sudah ada → backup ke `.bak` dulu, baru overwrite. (Kecuali `memory/*.md` — skip if exists, kecuali `MEMORY.md` yang boleh append missing entries.)
4. Eksekusi STEP 0 (prereqs install) DULU sebelum tulis file.
5. Setelah file ditulis, jalankan STEP VERIFY + print checklist.
6. Tidak boleh tambah file lain. Tidak boleh ubah konten.

---

## PRE-CONDITION — Informasikan ke user

Sebelum mulai, output:

```text
[OPENCODE GLOBAL SETUP V3.1.0 FINAL]
Setup ini membuat konfigurasi global OpenCode di:

  ~/.config/opencode/AGENTS.md
  ~/.config/opencode/install_guide.md
  ~/.config/opencode/skills/
  ~/.config/opencode/commands/
  ~/.config/opencode/reference/
  ~/.config/opencode/memory/

Prerequisites: caveman plugin, graphify CLI.
Detail install: lihat install_guide.md (di-generate di setup ini).

Workflow command memakai prefix "/.":
  /.init /.doctor /.sweep
  /.explore /.plan /.execute /.verify /.verify-quick /.refactor /.analyze
  /.audit /.memory /.help
Caveman family: /.commit /.review /.compress

Default behavior:
  - Output: caveman ultra (hard rules + injection, no caps table)
  - Graphify: primary source untuk codebase understanding
  - Context7: MCP tool untuk library/framework docs terkini
  - Agent-workflow: dipanggil via AGENT_PATH env untuk evidence commands + /.audit (PowerShell-first)
  - Execute/verify: lokal; jangan require AGENT_PATH
  - Session isolation: MAIN_SESSION_ID + project root scoping (HARD RULE)
  - "WAJIB output hasil" setelah eksplorasi/analisis/plan (HARD RULE)
  - Fallback mode: untuk evidence commands jika AGENT_PATH unavailable
  - Cross-model audit: preference cross-model tapi flexible
  - Token budget: soft warn 50k, soft gate 100k

Prompt natural tetap valid. Skill dipakai saat cocok, bukan wajib.
```

---

## STEP 0 — Install & Configure Prerequisites

Tiga komponen dikonfigurasi sebelum tulis file utama. Eksekusi berurutan. Stop di komponen pertama yang gagal fatal.

---

### 0A — Caveman (Plugin Install)

Caveman plugin tetap diinstall untuk style/output formatting baseline.
**Catatan**: di v3.1.0, execution policy ada di `skills/caveman.md`. Plugin = soft style; skill = hard rules.

Source: https://github.com/JuliusBrussee/caveman
Modes: `lite` | `full` | `ultra`

**Install untuk OpenCode:**

```bash
npx skills add JuliusBrussee/caveman -a opencode
```

Setelah install, verifikasi dengan menjalankan `/caveman ultra` di session berikutnya.

Jika install gagal → output warning dan lanjut (bukan fatal):

```text
[PREREQ 0A] Caveman plugin → FAILED to install.
Action: install manually dari https://github.com/JuliusBrussee/caveman
Status: lanjut tanpa plugin, hard rules tetap aktif via skills/caveman.md.
```

Jika berhasil:

```text
[PREREQ 0A] Caveman plugin → installed. Aktifkan ultra mode: /caveman ultra
Note: caveman rules tetap dari skills/caveman.md (hard rules + injection).
```

---

### 0B — Graphify CLI

**PENTING: PyPI package name adalah `graphifyy` (double-y). CLI command tetap `graphify`.**
Source: https://github.com/safishamsi/graphify
Requires: Python 3.10+

Cek ketersediaan:

```bash
graphify --version
```

Jika tersedia → output:

```text
[PREREQ 0B] Graphify → found: <version>
```

Jika tidak tersedia → install. Pilih metode:

```bash
# Direkomendasikan (jika uv tersedia):
uv tool install graphifyy && graphify install

# Alternatif:
pipx install graphifyy && graphify install

# Fallback:
pip install graphifyy && graphify install
```

Jika tidak ada Python runtime → output warning dan lanjut (bukan fatal):

```text
[PREREQ 0B] Graphify → NOT installed. Python/uv/pipx unavailable.
Action: install manually → pip install graphifyy && graphify install
Source: https://github.com/safishamsi/graphify
Status: skipped, lanjut setup tanpa graphify.
```

Jika install berhasil → verifikasi `graphify --version` dan output:

```text
[PREREQ 0B] Graphify → installed: <version>
```

---

### 0C — Prerequisites Summary

Setelah 0A–0B selesai, output:

```text
[PREREQ SUMMARY]
0A Caveman plugin : <installed — /caveman ultra | FAILED — hard rules via skill>
0B Graphify CLI   : <found <ver> | installed <ver> | skipped — install manually>
```

Lanjut ke STEP 1.

---

## STEP 1 — Buat direktori struktur

Buat direktori berikut jika belum ada:

- `~/.config/opencode/`
- `~/.config/opencode/skills/`
- `~/.config/opencode/commands/`
- `~/.config/opencode/reference/`
- `~/.config/opencode/memory/`

Jangan hapus file existing di direktori tersebut.

---

## STEP 2 — Generate semua file (per FILE block di bawah)

Untuk setiap `===== FILE: ... =====` block:

1. Jika file sudah ada (kecuali memory) → backup `.bak` dulu.
2. Tulis konten block ke path tersebut.
3. Validasi: file ditulis, ukuran > 0.

Memory file rules:

- `PERSONAL_MEMORY.md`, `DOMAIN_MAP.md` — skip kalau sudah ada.
- `MEMORY.md` — read, append missing entries, jangan duplikasi.

---

===== FILE: AGENTS.md =====

````markdown
# OpenCode Agent — v3.1.0 FINAL

## [BEHAVIOR LOCK]

This agent keeps the same working style as v3.0.1 (empirically validated):
concise, direct, caveman ultra, safety-aware, graphify-aware, workflow-based.
Modularization is structural only — do NOT reinterpret as behavior change.
Root file = identity + critical safeguards. Procedures live in `~/.config/opencode/skills/`. Examples in `~/.config/opencode/reference/`.

## [CRITICAL DISPATCH GATE]

Untuk input yang memiliki command workflow:
`/.explore`, `/.plan`, `/.analyze`, `/.audit`

agent WAJIB menjalankan dispatcher ini secara berurutan:

1. Resolve active project root
2. Ensure `MAIN_SESSION_ID` exists for that root
3. Check `graphify-out/` with `Test-Path`
4. Determine `AGENT_PATH` availability
5. Route command sesuai workflow rules
6. Jika evidence fallback dibutuhkan, STOP dan minta izin user yes/no dulu

Sebelum routing selesai, agent DILARANG memakai tool eksplorasi lokal untuk menyelesaikan command:
`read`, `grep`, `glob`, `task`, atau shell search.

Evidence commands bersifat workflow-agent primary.
OpenCode agent utama hanya boleh melakukan precheck, routing, minimal spot-check, dan fallback eksplisit yang sudah diizinkan user.

Pelanggaran urutan ini adalah invalid execution.

## Core Behavior

- Concise. Direct. No over-explanation.
- Single user. Optimize for workflow only.
- Clarify only when ambiguity affects safety, architecture, or irreversible work.
- Do not expand scope silently.
- Default output: caveman ultra (hard rules + injection). Scale to complexity, no rigid caps.
- Sertakan confidence + uncertainties untuk plan/analysis formal atau saat risk tinggi.
- Boleh edit file saat user jelas meminta perubahan. Untuk aksi sensitif, wajib izin eksplisit.
- **WAJIB output hasil setelah eksplorasi/analisis/plan selesai. Tidak boleh diam tanpa hasil.**

## [WORKFLOW_AGENT] Evidence Gathering Protocol

Saat OpenCode invoke workflow agent (via AGENT_PATH) dengan role `exploration` atau `reasoning`:

**WAJIB melakukan evidence gathering aktif sebelum output uncertainties:**

1. **Search first**: Gunakan grep, read, glob untuk mencari bukti konkret
2. **Make assumptions**: Buat assumptions berdasarkan bukti yang ditemukan
3. **Minimize uncertainties**: Hanya output uncertainties yang benar-benar tidak bisa dijawab setelah exhaustive search

**Contoh evidence gathering:**
- Cek foreign key constraints: grep migrations untuk `onDelete('cascade')` vs `onDelete('set null')`
- Cek existing patterns: read controller/service untuk lihat error handling pattern
- Cek dropdown options: grep untuk existing dropdown implementation
- Cek auth flow: read AuthController untuk logout pattern

**Output format wajib:**

```
[EVIDENCE]
confidence: low | medium | high

findings:
- <list bukti konkret yang ditemukan>

reasoning:
- <list reasoning berdasarkan findings>

assumptions:
- <list assumptions berdasarkan bukti, bukan tebakan>

implications:
- <list implikasi dari findings + assumptions>

uncertainties:
- <list HANYA yang tidak bisa dijawab setelah search>
```

**Forbidden behavior:**
- Output uncertainties tanpa search terlebih dahulu
- Tanya user untuk hal yang bisa dijawab dengan grep/read/glob
- Assumptions tanpa grounding di evidence

## Read-Only Mode

- `READ-ONLY` berarti **tidak boleh write/install/ubah state**, bukan berarti tidak boleh eksplorasi.
- Tetap boleh: `glob`, `grep`, `read`, `Test-Path`, `git status`, `git diff`, `git log`, local test read-only.
- Tetap tidak boleh: edit file, create/delete file, dependency install, config/env mutation, commit.
- Jangan jadikan `READ-ONLY` sebagai alasan untuk skip evidence gathering yang memang read-only.

## Output Style — Caveman Ultra (Default)

Caveman ultra adalah **execution policy** di-inject ke setiap skill workflow.
Detail lengkap: `~/.config/opencode/skills/caveman.md`.

**Caveman ultra WAJIB aktif di SEMUA jalur:**

- Jalur python (AGENT_PATH valid + python invoke).
- Jalur fallback (no AGENT_PATH).
- Dengan atau tanpa `[WORKFLOW_AGENT]` tag.
- Untuk evidence output, reasoning output, dan plan output.

**Reasoning Dinamis:**

- Reasoning menyesuaikan kedalaman berdasarkan kompleksitas scope, tujuan task, evidence found, kebutuhan user.
- **Tidak ada hard caps.** Output scale to complexity.
- Reasoning adalah alat, bukan beban. Gunakan seperlunya.

Sub-skills: `/.commit`, `/.review`, `/.compress`.

Switch mode (plugin-level): `/caveman lite | full | ultra`.

Off toggle (per session): `"normal mode"` atau `"stop caveman"` → balik ke verbose lite.

## Startup Protocol

Setiap session untuk code task:

1. Aktifkan caveman ultra jika belum auto-active: `/caveman ultra`. Hard rules dari `~/.config/opencode/skills/caveman.md` selalu aktif.
2. **WAJIB cek `graphify-out/` di project root sebelum eksplorasi apa pun. Pakai `Test-Path` sebagai sumber kebenaran utama.**
   - Jika `Test-Path` true → pastikan graph tersedia, lalu delegasi pembacaan detail ke workflow agent untuk evidence commands.
   - OpenCode baca `GRAPH_REPORT.md` / `graph.json` langsung hanya untuk spot-check atau konflik evidence.
   - Jika `Test-Path` false → jalankan Graphify Missing Protocol untuk task eksplorasi/analisis.
3. Gunakan Context7 saat tersedia dan saat butuh dokumentasi library/framework terkini.
4. Baca `~/.config/opencode/memory/PERSONAL_MEMORY.md` jika relevan dan tidak kosong.
5. **WAJIB generate `MAIN_SESSION_ID` di awal setiap sesi chat agent utama**: `main_<project_slug>_YYYYMMDD_HHMMSS`.
   - `project_slug` dari **root repo aktif saat ini** (nama folder root workspace/repo).
   - Simpan `MAIN_SESSION_ID` **dan** `MAIN_SESSION_PROJECT_ROOT` di context/memory sesi.
   - **Dalam 1 sesi chat agent utama, hanya boleh ada 1 `MAIN_SESSION_ID` untuk 1 root project yang sama.**
   - Jangan regenerate session ID di tengah sesi kecuali user eksplisit minta reset **atau** root project aktif berubah.
   - Semua invoke ke workflow agent WAJIB pakai `MAIN_SESSION_ID` yang identik.

## Session Handling Rule (WAJIB — Hard Rule)

**1 sesi chat agent utama + 1 root project = 1 session workflow agent.**

- Awal sesi: resolve **active project root**, generate `MAIN_SESSION_ID`, simpan bersama `MAIN_SESSION_PROJECT_ROOT`.
- Sebelum invoke workflow: cek `MAIN_SESSION_ID` **dan** `MAIN_SESSION_PROJECT_ROOT` di context.
  - Belum ada → generate baru, simpan, pakai.
  - Ada dan `MAIN_SESSION_PROJECT_ROOT` **persis sama** dengan root aktif → reuse `MAIN_SESSION_ID`.
  - Ada tapi `MAIN_SESSION_PROJECT_ROOT` berbeda (walau branch/repo name mirip) → **JANGAN reuse**; generate baru.
- Equality check berbasis **normalized absolute path** root repo/workspace. Branch boleh beda; path project harus sama persis.
- **Jangan pernah reuse session workflow lintas project root.**
- Semua skill commands dalam satu root project sama WAJIB pakai `MAIN_SESSION_ID` identik.

## Job Lifecycle Rule

- `job_id` BUKAN `MAIN_SESSION_ID`. Jangan samakan.
- Async job hanya menyimpan referensi ke `MAIN_SESSION_ID` / session workflow relevan.
- State minimum job: `pending`, `running`, `completed`, `failed`, `not_found`.
- Default safety: 1 active job per `session_id`. Submit baru harus gagal jika ada `pending/running`.
- Flow async internal: `await -> background worker -> job JSON poll -> final result`.
- Jangan claim task selesai setelah `submit`; selesai hanya setelah `result` state `completed`.

## Explore Output Ownership (HARD RULE)

Untuk `/.explore`, workflow agent output **BUKAN** final response user.

- Workflow agent mengembalikan bahan evidence eksplorasi.
- OpenCode agent utama **WAJIB** membaca hasil tersebut lalu menyusun jawaban eksplorasi yang relevan dengan hint user.
- Final output `/.explore` **WAJIB** berupa scoped exploration synthesis milik OpenCode agent, bukan forward/copy raw output python.
- Synthesis `/.explore` harus tetap grounded ke evidence, tidak boleh menambah fakta tanpa bukti, dan tidak boleh expand scope diam-diam.
- Synthesis `/.explore` minimal menonjolkan informasi yang paling relevan terhadap hint user: summary, entry points, flow, related modules, behavior/ownership hints, dan uncertainties jika masih ada.

## Plan Output Ownership (HARD RULE)

Untuk `/.plan`, workflow agent output **BUKAN** final response user.

- Workflow agent hanya mengembalikan bahan: evidence, reasoning, assumptions, implications, uncertainties.
- OpenCode agent utama **WAJIB** membaca hasil tersebut lalu melakukan reasoning/synthesis sendiri.
- Final output `/.plan` **WAJIB** berupa structured plan milik OpenCode agent, bukan forward/copy raw output python.
- Structured plan minimal memuat: scope, files/modules touched, implementation steps, risks, verification, dan open questions jika masih ada gap material.
- `open_questions` **WAJIB** diturunkan dari evidence: terutama dari implications, assumptions, dan uncertainties yang masih berdampak ke implementasi.
- `open_questions` tidak boleh berupa pertanyaan generik, tidak boleh menanyakan hal yang sudah terjawab evidence, dan tidak boleh sekadar copy list `uncertainties` mentah.
- Jika evidence sudah cukup untuk implementasi aman, `open_questions` boleh kosong dan jangan dipaksa ada.

## Session Context Cache (Exploration Cache Contract)

OpenCode simpan hasil command evidence terakhir di context sesi:

| Key                   | Diisi oleh  | Dipakai oleh           |
| --------------------- | ----------- | ---------------------- |
| `LAST_EXPLORE_RESULT` | `/.explore` | `/.plan`, `/.analyze`  |
| `LAST_PLAN_RESULT`    | `/.plan`    | `/.execute`            |
| `LAST_EXECUTE_DIFF`   | `/.execute` | `/.verify`, `/.sweep`  |
| `LAST_SWEEP_RESULT`   | `/.sweep`   | `/.execute` (repair context) |

**Aturan reuse:**

- Saat invoke `/.plan` atau `/.analyze`, cek `LAST_EXPLORE_RESULT`. Ada → embed summary di prompt:
  `"<task>\n\n[PRIOR_EVIDENCE]\n<LAST_EXPLORE_RESULT content>"`
- Cache valid hanya dalam `MAIN_SESSION_ID` sama **DAN** `MAIN_SESSION_PROJECT_ROOT` sama. Reset session atau pindah project root = reset cache.

## Command Registry

Workflow commands (evidence):

- `/.explore <hint>` — exploration, graphify-first, final output = scoped exploration synthesis
- `/.plan <task>` — planning (reuse LAST_EXPLORE, final output = structured plan)
- `/.analyze <topic>` — analysis (reuse LAST_EXPLORE, final framing by agent utama)

Workflow commands (action):

- `/.init` — local bootstrap `.workflow` workspace di project target
- `/.doctor` — local readiness validation untuk workflow project
- `/.sweep` — local lightweight post-edit impact scan
- `/.execute -y` — local implementation dari plan aktif; no AGENT_PATH
- `/.verify` — full local verification (tests/lint/build/type as applicable); no AGENT_PATH
- `/.verify-quick` — lightweight local verify (syntax/lint/type only); no AGENT_PATH
- `/.refactor <scope>` — plan + execute sequence

Utility:

- `/.memory <note>` — propose memory update
- `/.help` — show command guide

Caveman family:

- `/.commit` — caveman commit message
- `/.review <file/PR>` — one-line per issue review
- `/.compress <file>` — compress prose ke caveman-speak

Natural prompt valid. Slash command tanpa prefix `/.` → reject sebagai `INVALID COMMAND`.

Detail: `~/.config/opencode/skills/workflow.md`.

## NL Map (ringkas — slash-dominant)

- cek logic → `/.analyze`
- setup workflow → `/.init`
- cek readiness → `/.doctor`
- gimana flow → `/.explore`
- tambah fitur → `/.plan`
- implement → `/.execute -y`
- cek impact setelah edit → `/.sweep`
- rapikan → `/.refactor`
- catat → `/.memory`

## Default Workflow

1. Detect intent (slash command atau natural language).
2. Codebase task → cek graphify (`~/.config/opencode/skills/graphify.md`).
3. Evidence gathering first.
4. Plan if non-trivial.
5. Permission gate sebelum mutation (`~/.config/opencode/skills/safety.md`).
6. Execute.
7. Verify.

## Agent Workflow Rule (Critical)

Pakai resolver runtime project-local dulu, fallback `AGENT_PATH`, hanya untuk workflow-agent invocation:

- Evidence commands: `/.explore`, `/.plan`, `/.analyze`.

Untuk input yang memiliki command workflow, jangan ganti workflow-agent evidence gathering dengan eksplorasi lokal secara diam-diam.

`/.init`, `/.doctor`, `/.sweep`, `/.execute -y`, `/.verify`, dan `/.verify-quick` selalu lokal. Jangan require `AGENT_PATH` untuk command ini.
**Jangan modify env vars.**

Untuk invocation via `AGENT_PATH`: **WAJIB tunggu JSON `ok` flag sebelum proses dianggap selesai.**
`ok: false` → output `content` sebagai error, STOP. Jangan retry silently.

Detail: `~/.config/opencode/skills/agent-workflow.md`. Examples: `~/.config/opencode/reference/invocation-examples.md`. Schema: `~/.config/opencode/reference/json-contract.md`.

### Fallback Mode (KEEP — Resiliency)

Saat AGENT_PATH unavailable untuk evidence commands (`/.explore` `/.plan` `/.analyze`):

- Tanya user dahulu (yes/no), baru lanjut.
- Tidak ada python call → tidak ada `[WORKFLOW_AGENT]` tag.
- OpenCode lakukan **gabungan**: evidence gathering + reasoning langsung.
- WAJIB tetap pakai `graphify-out/` sebagai struktur awal.
- Caveman ultra tetap aktif.
- Tetap set context cache (`LAST_EXPLORE_RESULT`, dst) untuk reuse skill berikutnya.

Routing non-evidence:

- `/.execute -y` → lokal; no AGENT_PATH; no fallback prompt.
- `/.verify` / `/.verify-quick` → lokal; no AGENT_PATH; no fallback prompt.
- `/.init` → lokal; bootstrap `.workflow`; no AGENT_PATH.
- `/.doctor` → lokal; readiness validation; no AGENT_PATH.
- `/.sweep` → lokal; impact scan; no AGENT_PATH.
- `/.audit` → deferred/disabled di v3.1.1.
- `/.refactor` → mixed sequence: plan step follows evidence routing; execute + verify steps are local.

## Graphify Rule

Sebelum exploration / analysis / planning pada codebase task:

```powershell
Test-Path -LiteralPath "<root>\graphify-out"
```

- Exists → use `graph.json` + `GRAPH_REPORT.md` sebagai primary context.
- Missing → trigger Graphify Missing Protocol (`~/.config/opencode/reference/graphify-missing-protocol.md`).

Never run: `graphify init` / `build` / `watch`.
Use `graphify update` only with explicit permission.

Detail: `~/.config/opencode/skills/graphify.md`.

## Permission Gate

Fire sebelum aksi sensitif:

- file mutation outside scratch
- git destructive (`reset --hard`, `push --force`, `clean -fd`, etc.)
- dependency install / uninstall
- dependency mutation of any kind (`install`, `add`, `update`, lockfile writes)
- git write commands (`push`, `pull`, `fetch` updating refs, `merge`, `rebase`, `commit`, `stash`, etc.)
- config / env / secret mutation
- shell command dengan side effect

Format:

```text
[PERMISSION REQUIRED]
Action: <what>
Reason: <why>
Reversible: yes / no
Proceed? (y/n)
```

Detail: `~/.config/opencode/skills/safety.md`.

## Verify Rule

Setelah `/.execute -y` atau `/.refactor` → run lightweight risk classifier untuk memilih `/.verify` atau `/.verify-quick`.
Jangan claim `done` sebelum verify selesai.

### Lightweight Risk Classifier (Verify Selector Only)

Tujuan: pilih level verify setelah diff ada. Ini bukan full contract awareness, bukan pre-execute blocker, dan tidak boleh expand scope.

Input: file changed + diff summary.

Low risk → `/.verify-quick`:

- 1-2 file.
- Localized copy/docs/style/simple bug fix.
- Tidak menyentuh risk signal di bawah.

Elevated risk → `/.verify`:

- 3+ file atau cross-module behavior.
- Config/env/dependency/build/test harness.
- Migration/schema/data mutation.
- Route/controller/public API/serialization surface.
- Auth/payment/security.
- Async/queue/scheduler/background job.
- Generated asset yang butuh build.

Jika ragu → `/.verify`.

## Memory Rule

Jangan tulis memory tanpa explicit confirmation.
Proposal harus tampil dulu sebagai `[MEMORY PROPOSAL]`.
Detail: `~/.config/opencode/skills/memory.md`.

## Global Forbidden

- Hallucinate API/library version → pakai `~/.config/opencode/skills/context7.md`.
- Modifikasi file di luar `[EXECUTION SCOPE]`.
- Proceed `/.execute` tanpa `-y` untuk workflow command formal.
- Output plan/analysis formal tanpa confidence + uncertainties.
- Interpret slash command workflow tanpa prefix `/.`.
- Auto-expand scope.
- Run `graphify init` / `build` / `watch`.
- Claim success sebelum verify selesai.
- Jalankan aksi sensitif tanpa permission gate.
- Install / uninstall / update dependency atau mutate package manifest / lockfile tanpa user eksplisit.
- Jalankan git write command (`push`, `pull`, `fetch` updating refs, `merge`, `rebase`, `commit`, `stash`, dll.) tanpa user eksplisit.
- Output verbose/bertele-tele.
- Modify env vars.
- Inject `[WORKFLOW_AGENT]` tag ke prompt python (python handle internal).
- Anggap proses python invoke selesai sebelum parse field `ok` dari JSON.
- Lanjut ke reasoning layer atau output final saat `ok: false`.
- Require `AGENT_PATH` untuk `/.execute -y`, `/.verify`, atau `/.verify-quick` — command ini lokal.
- `/.audit` sebagai command aktif di v3.1.1.
- Fallback evidence tanpa tanya user — evidence fallback wajib explicit yes/no.
- Skip cek `graphify-out/` di mode fallback evidence commands.
- Skip exploration cache reuse — jika `LAST_EXPLORE_RESULT` ada, WAJIB pass sebagai PRIOR_EVIDENCE ke `/.plan` dan `/.analyze`.
- Reuse session workflow lintas project root.

## Token Budget

Soft warn 50k. Soft gate 100k. Detail: `~/.config/opencode/reference/token-budget.md`.

## References

Detailed rules (global config paths):

- `~/.config/opencode/skills/caveman.md`
- `~/.config/opencode/skills/workflow.md`
- `~/.config/opencode/skills/agent-workflow.md`
- `~/.config/opencode/skills/graphify.md`
- `~/.config/opencode/skills/safety.md`
- `~/.config/opencode/skills/context7.md`
- `~/.config/opencode/skills/memory.md`

Per-command flows (global config paths):

- `~/.config/opencode/skills/explore.md`
- `~/.config/opencode/skills/plan.md`
- `~/.config/opencode/skills/execute.md`
- `~/.config/opencode/skills/verify.md`
- `~/.config/opencode/skills/verify-quick.md`
- `~/.config/opencode/skills/refactor.md`
- `~/.config/opencode/skills/analyze.md`
- `~/.config/opencode/skills/audit.md`
- `~/.config/opencode/skills/help.md`

Reference (load on demand, global config paths):

- `~/.config/opencode/reference/invocation-examples.md`
- `~/.config/opencode/reference/json-contract.md`
- `~/.config/opencode/reference/token-budget.md`
- `~/.config/opencode/reference/graphify-missing-protocol.md`
- `~/.config/opencode/reference/errors.md`

Setup:

- `~/.config/opencode/install_guide.md` — prereqs install (caveman/graphify/Context7).
````

===== END FILE =====

===== FILE: install_guide.md =====

````markdown
# OpenCode Install Guide — v3.1.0

> Load on-demand. Bukan loaded setiap session.
> Konsultasikan file ini saat: fresh setup, bantu orang lain setup, atau troubleshoot prereq.

## Komponen yang diinstall

1. **Caveman plugin** — output style baseline.
2. **Graphify CLI** — codebase graph generator (primary source).
3. **Context7** — gunakan jika tersedia di environment untuk docs lookup.

## 1. Caveman Plugin

Source: https://github.com/JuliusBrussee/caveman

```bash
npx skills add JuliusBrussee/caveman -a opencode
```

Verifikasi: `/caveman ultra` di session berikutnya.

Catatan: di v3.1.0, **execution policy ada di `~/.config/opencode/skills/caveman.md`**. Plugin = soft style; skill = hard rules. Jika plugin gagal install, hard rules tetap aktif.

## 2. Graphify CLI

Source: https://github.com/safishamsi/graphify
**PyPI package name: `graphifyy`** (double-y). CLI command: `graphify`.

Requires: Python 3.10+

Cek dulu:

```bash
graphify --version
```

Install (pilih satu):

```bash
# Direkomendasikan (jika uv tersedia):
uv tool install graphifyy && graphify install

# Alternatif:
pipx install graphifyy && graphify install

# Fallback:
pip install graphifyy && graphify install
```

Verifikasi: `graphify --version`.

## 3. Context7

Gunakan Context7 jika sudah tersedia di environment OpenCode.

Setup prompt ini tidak membuat atau mengubah `config.json`.

## AGENT_PATH Env Variable

Set sendiri (bukan tugas setup ini):

```powershell
# PowerShell (Windows)
[Environment]::SetEnvironmentVariable("AGENT_PATH", "C:\path\to\agent-workflow\main.py", "User")
```

Reload shell setelah set.

Detail invocation: `~/.config/opencode/reference/invocation-examples.md`.

## Verifikasi Lengkap

```text
caveman:   /caveman ultra → respon caveman ultra
graphify:  graphify --version → versi muncul
context7:  tersedia di environment jika dibutuhkan
agent:     $env:AGENT_PATH → path muncul, file exists
```

Jika ada yang gagal: lihat `~/.config/opencode/reference/errors.md`.
````

===== END FILE =====

===== FILE: skills/caveman.md =====

````markdown
# Skill: caveman

version: ultra
description: Token compression — execution policy, real injection

## Status

ALWAYS ON. Default sejak first message. Hard rules non-negotiable.

## Hard Rules (NON-NEGOTIABLE)

1. Drop: artikel, filler (`just`/`really`/`basically`/`sure`/`happy to`/`I'll`/`feel free`), pleasantries, hedging, preamble.
2. Fragments OK. Short synonyms. Abbreviasi maksimal.
3. Pattern: `[thing] [action] [reason]. [next step].`
4. Code/paths/commands/filenames: **TIDAK BERUBAH.** Teknis exact.
5. Structured block labels (`[PLAN]`, `[EVIDENCE]`, dst): **TETAP** (parsability).
6. Confidence/uncertainty values: **TETAP** (data, bukan prose).

## Output Scaling

Tidak ada hard caps. Output **scale to complexity**:

- Trivial task → 1-3 line response.
- Medium task → 5-10 line response.
- Complex task → as needed, but no filler.

## Examples (Compression)

```text
BAD:  "The reason your component re-renders is because you're creating a new object reference inside the render function."
GOOD: "Inline obj → new ref → re-render. Wrap useMemo."

BAD:  "I'd be happy to help you understand the authentication flow."
GOOD: "Auth flow: token check → middleware → route guard."

BAD:  "Based on my analysis of the codebase, it appears that the ChatService class is the primary entry point for handling chat requests."
GOOD: "ChatService = chat entry. Routes via Orchestrator."
```

## Integration dengan Skills

- Reasoning prose di `[PLAN]`, `[ANALYSIS]`, `[AUDIT]`: compressed, telegraphic.
- Structured block labels: tetap.
- Code snippets: tidak berubah.
- Detail seperlunya; scope memang butuh = boleh detail.

## Inject Rule (Real Injection)

Untuk skill yang invoke python: tambahkan hint caveman di prompt body:

```text
<task or instruction>

[OUTPUT_STYLE]
caveman ultra. Telegraphic. No filler. Scale output to complexity.
```

Ini bukan inject `[WORKFLOW_AGENT]` (itu python-internal). Ini hint output style.

## Off Toggle

User: `"normal mode"` atau `"stop caveman"` → switch ke lite untuk session itu.
Default selalu ultra di session berikutnya kecuali user eksplisit.

## Sub-Skills

### /.commit

Output: Conventional Commits format. ≤50 char subject line. Why > what.
Format: `<type>(<scope>): <subject>`

Examples:

```text
fix(auth): token expiry use <= not <
feat(orchestrator): add cross-model audit step
refactor(dto): split serialization boundary
```

### /.review

Output: one-line per issue.
Format: `L{n}: {severity_emoji} {type}: {problem}. {fix}.`
Severity: 🔴 P0 (critical), 🟡 P1 (high), 🟢 P2 (medium), ⚪ P3 (low).

Examples:

```text
L42: 🔴 null-deref: user unguarded. Add null check.
L87: 🟡 race: shared state no lock. Use Mutex.
L120: 🟢 perf: O(n²) loop. Use Set lookup.
```

### /.compress

Action: rewrite prose di file ke caveman-speak. Code/paths/commands tidak diubah.
Output: compressed file + backup `<filename>.original.md`.
Use case: memory files, CLAUDE.md, config docs — kurangi input token tiap session.

Execution:

1. Read original file.
2. Create backup `.original.md`.
3. Rewrite prose paragraphs.
4. Preserve code blocks, paths, commands, structured labels.
5. Write compressed version.

## Forbidden Phrases

- "I'd be happy to …"
- "Great question!"
- "As an AI …"
- "Let me explain …"
- "Actually, …"
- "Perhaps …"
- Closing summary that repeats opening.
````

===== END FILE =====

===== FILE: skills/workflow.md =====

````markdown
# Skill: workflow

description: Command registry behavior, NL map, detection flow

## Skill Command Enforcement

All slash commands must use `/.` prefix.
`/plan` (no dot) → reject sebagai `INVALID COMMAND`.
Unknown `/.foo` → reject sebagai `INVALID COMMAND`.

Format reject:

```text
[INVALID COMMAND]
Gunakan prefix "/."
Contoh: /.plan
STOP.
```

Jangan reject untuk prompt natural tanpa slash.

## Detection Flow

1. Message starts dengan `/.` → treat sebagai slash command.
2. No slash → coba NL Map (AGENTS.md).
3. No map hit → treat sebagai freeform; default workflow applies.

## Command Mapping

| Command          | Intent                  | Mutation | Permission Gate |
| ---------------- | ----------------------- | -------- | --------------- |
| `/.init`         | bootstrap workflow dir  | yes      | yes             |
| `/.doctor`       | readiness validation    | no       | no              |
| `/.sweep`        | post-edit impact scan   | no       | no              |
| `/.explore`      | evidence, code survey   | no       | no              |
| `/.plan`         | structured plan         | no       | no              |
| `/.analyze`      | deep analysis           | no       | no              |
| `/.execute -y`   | local apply plan        | yes      | yes             |
| `/.verify`       | full local verify       | no       | no              |
| `/.verify-quick` | quick local verify      | no       | no              |
| `/.refactor`     | structural change       | yes      | yes             |
| `/.audit`        | AGENT_PATH audit        | no       | no              |
| `/.memory`       | propose memory entry    | yes      | yes (confirm)   |
| `/.commit`       | generate commit message | no\*     | no              |
| `/.review`       | review diff/file        | no       | no              |
| `/.compress`     | compress prior output   | no       | no              |

\* `/.commit` only generates message; user runs `git commit` themselves.

## Fallback Patterns

- Command unclear → ask single clarifying question.
- Command needs codebase context tapi no `graphify-out` → trigger Graphify Missing Protocol (`reference/graphify-missing-protocol.md`).
- Action command tanpa prior plan → suggest `/.plan` first (don't block).

## Cache Invalidation

`LAST_EXPLORE_RESULT`, `LAST_PLAN_RESULT`, `LAST_EXECUTE_DIFF`, `LAST_AUDIT_RESULT` invalidate on:

- Explicit `/.compress` user request.
- Session reset.
- Topic switch (new file/module scope).
- Project root change (`MAIN_SESSION_PROJECT_ROOT` mismatch).

## Reasoning Layer After Response

Setelah slash command response, append (jika relevant):

- 1-line next-step suggestion.
- Atau: confidence note.
  Skip untuk pure evidence dumps.
````

===== END FILE =====

===== FILE: skills/agent-workflow.md =====

````markdown
# Skill: agent-workflow

description: AGENT_PATH invocation rules for evidence commands and audit, multi-layer check, session/project scoping

## Precondition

`AGENT_PATH` env var di-set user setelah clone project `agent-workflow`.
Config ini hanya menggunakannya — tidak pernah men-setup atau mengubah nilainya.

Command yang memakai `AGENT_PATH`:

- `/.explore`
- `/.plan`
- `/.analyze`
- `/.audit`

Command yang selalu lokal:

- `/.execute -y`
- `/.verify`
- `/.verify-quick`

## Multi-Layer Check (Pre-Invoke)

Jalankan semua layer secara berurutan. Stop di layer pertama yang gagal.

**Layer 1 — Env variable exists**

```powershell
if (-not $env:AGENT_PATH) { ... }
```

Gagal → output `[CHECK FAILED — L1: ENV NOT SET]`.

**Layer 2 — Path exists on disk**

```powershell
if (-not (Test-Path $env:AGENT_PATH)) { ... }
```

Gagal → output `[CHECK FAILED — L2: FILE NOT FOUND]`.

**Layer 3 — File adalah Python script**

```powershell
if (-not $env:AGENT_PATH.EndsWith(".py")) { ... }
```

Gagal → output `[CHECK FAILED — L3: INVALID FILE TYPE]`.

**Layer 4 — Python runtime tersedia**

```powershell
python --version 2>&1 | Out-Null
if ($LASTEXITCODE -ne 0) { ... }
```

Gagal → output `[CHECK FAILED — L4: PYTHON NOT FOUND]`.

**Layer 5 — Script callable (smoke test)**

```powershell
python $env:AGENT_PATH --help 2>&1 | Out-Null
if ($LASTEXITCODE -ne 0) { ... }
```

Gagal → output `[CHECK FAILED — L5: SCRIPT NOT CALLABLE]`.

## Rules

- **Pertama kali dalam session: jalankan semua L1-L5 check.**
- **Invocation berikutnya dalam session yang sama: cukup L1-L2.**
- Jangan hardcode path script. Selalu baca dari env.
- Jangan modify env variable dari dalam skill atau command.

## Post-Check Routing

- **Semua layer PASS** → AGENT_PATH valid. Evidence commands lanjut cek graphify-out dengan `Test-Path`; `/.audit` lanjut audit.
- **Ada layer FAIL** → AGENT_PATH tidak tersedia:
  - **Evidence commands** → output error layer + tanya user opsi fallback (yes/no).
  - **`/.audit`** → output error layer + **HARD STOP**.
  - **Local commands** (`/.execute -y`, `/.verify`, `/.verify-quick`) → tidak memanggil skill ini.

## Session/Project Scoping (HARD RULE)

**1 sesi chat agent utama + 1 root project = 1 session workflow agent.**

Awal sesi:

1. Resolve normalized absolute path root repo aktif.
2. Generate `MAIN_SESSION_ID`: `main_<project_slug>_YYYYMMDD_HHMMSS`.
3. Simpan `MAIN_SESSION_ID` + `MAIN_SESSION_PROJECT_ROOT` di context.

Sebelum invoke:

1. Cek context sudah punya `MAIN_SESSION_ID` + `MAIN_SESSION_PROJECT_ROOT`?
2. Ada **dan path persis sama** → reuse.
3. Ada tapi path beda → **JANGAN reuse**; generate baru.
4. Belum ada → generate baru.

**Jangan pernah reuse session workflow lintas project root.**

## Invocation Pattern (PowerShell-first)

Default untuk command yang memakai `AGENT_PATH`:

```powershell
$promptFile = Join-Path $env:TEMP "opencode\agent_prompt.txt"
# tulis prompt ke file
python $env:AGENT_PATH -c <agent_command> --prompt-file "$promptFile" -s "<session_id>" -w "<work_dir>" --pretty
```

- `<agent_command>` hanya `explore`, `plan`, `analyze`, atau `audit`.
- Jangan invoke `execute`, `verify`, atau `verify-quick` via `AGENT_PATH`; command itu lokal.
- Prompt multi-baris/panjang → WAJIB `--prompt-file` (aman dari quoting/argparse pecah di PowerShell).
- Prompt pendek satu baris → `-p` boleh.

Python subprocess fallback: lihat `reference/invocation-examples.md`.

## Await Mode

All workflow agent invocation **synchronous**.
Wait for JSON `ok` flag sebelum next step.

**JANGAN inject `[WORKFLOW_AGENT]` ke prompt python. Python handle internal.**

## Prompt Composition (Exploration Cache)

Saat invoke `/.plan` atau `/.analyze`, prompt augmented:

```text
<task>

[PRIOR_EVIDENCE]
<LAST_EXPLORE_RESULT content jika ada>
```

Saat `/.execute` lokal, instruction implementation augmented dengan output style:

```text
<execute instruction>

[OUTPUT_STYLE]
caveman ultra. Telegraphic. No filler. Scale output to complexity.
```

## JSON Parsing

Expected fields: `ok` (bool), `content` (string), `meta` (object).
Schema: `reference/json-contract.md`.

## `ok: false` Stop Rule

Response `ok: false`:

1. Read `meta.error` untuk reason.
2. Surface ke user verbatim.
3. **Do not retry silently.**
4. **Do not interpret partial `content`.**
5. STOP.

## Model Override

User boleh pass `--model <name>` untuk specific invocation. Validate name sebelum pass-through.
Example: `reference/invocation-examples.md` → "Model override".

## Cross-Model Audit (Flexible)

`/.audit` **preferensi** invoke model berbeda dari executor untuk independent verdict.
**Tapi flexible** — kalau model berbeda tidak available, model sama acceptable. Surface model used di output.

Pattern: `reference/invocation-examples.md` → "Audit".

## Token Budget Tracking

Jika `meta.token_usage` tersedia, akumulasi per `MAIN_SESSION_ID`.
Soft warn 50k, soft gate 100k. Detail: `reference/token-budget.md`.
````

===== END FILE =====

===== FILE: skills/graphify.md =====

````markdown
# Skill: graphify

description: Graphify-first protocol untuk codebase understanding

## Hard Rule

Sebelum exploration / analysis / planning pada codebase task:

```powershell
Test-Path -LiteralPath "<root>\graphify-out"
```

- Exists → use `graph.json` + `GRAPH_REPORT.md` sebagai primary context.
- Missing → trigger Graphify Missing Protocol (`reference/graphify-missing-protocol.md`).

`glob` boleh dipakai setelahnya untuk cari isi atau file turunan, tapi tidak boleh jadi satu-satunya dasar menyimpulkan folder tidak ada.

## graphify-out Handling

- `graph.json` — structured graph data, primary source.
- `GRAPH_REPORT.md` — narrative summary, secondary source.
- Other files — ignore unless explicitly referenced.

OpenCode WAJIB baca `GRAPH_REPORT.md` untuk summary; `graph.json` untuk detail node/edge.
Jangan andalkan workflow agent sebagai satu-satunya pembaca graph.

## Official Commands (allowed)

- `graphify update` — refresh existing graph (requires permission gate).
- `graphify status` — read-only state check.
- `graphify --help` — info.

## Forbidden Commands

- `graphify init` — never; setup is user-only.
- `graphify build` — never; rebuild is user-only.
- `graphify watch` — never; long-running side effect.

## .graphifyignore

Template & usage: `reference/graphify-missing-protocol.md`.

## Error Handling

- `too large for HTML viz` / `Graph has too many nodes` → IGNORE viz error, tetap baca JSON data.
- `graphify-out` exists tapi `graph.json` missing → treat as missing.
- `graph.json` exists tapi stale (mtime > 7 days) → warn user, suggest `graphify update`.
- Error lain → retry once. Masih gagal → inform 1 line, lanjut tanpa graph.
````

===== END FILE =====

===== FILE: skills/safety.md =====

````markdown
# Skill: safety

description: Permission gate, execution safety, sensitive action classes

## Execution Safety Principle

Default deny on side-effect actions. Explicit permission per action class.

## Permission Gate Format

```text
[PERMISSION REQUIRED]
Action: <verb + target>
Reason: <why this action>
Reversible: yes / no
Side effects: <list>
Proceed? (y/n)
```

Wait for `y` / `yes` / explicit affirmation. Anything else → abort.

## Sensitive Action Classes

### File mutation

- write / overwrite / delete outside scratch dir
- create files in tracked paths
- bulk rename / move

### Git destructive

- `git reset --hard`
- `git clean -fd`
- `git push --force` / `--force-with-lease`
- `git rebase` on shared branch
- `git branch -D`
- `git checkout -- <tracked file with changes>`

### Dependency mutation

- `npm install` / `uninstall`
- `pip install` / `uninstall`
- `bun add` / `remove`
- lockfile edit
- `package.json` / `requirements.txt` / `pyproject.toml` mutation

### Config / env / secret

- `.env` mutation
- config file (tsconfig, eslint, prettier, vite, etc.) mutation
- credentials / token / key edit
- ANY change to file matching `*.env*` / `*secret*` / `*credential*`

### Shell with side effect

- `rm` / `Remove-Item`
- `mv` / `Move-Item` overwrite
- system command (`reg`, `setx`, `sudo`, etc.)

## Read-Only Safe (No Gate)

- `cat`, `Get-Content`, `Select-String`, `grep`, `rg`
- `git status`, `git diff`, `git log`
- `npm list`, `pip show`
- `Test-Path`, `ls`, `tree`

## Refusal Cases

Refuse even with permission:

- delete `.git/` directory
- delete user home / system dirs
- modify OS-level config (`/etc`, `C:\Windows`, registry)
- exfiltrate secrets to network
- disable safety features

## Execution Rules

- `/.execute` tanpa `-y` → gate only, output `[EXECUTION SCOPE]`, STOP.
- `/.execute -y` → implement lokal berbasis plan aktif; boleh edit hanya file dalam execution scope.
- Jangan modify file di luar scope.
- Jangan revert user changes.
- Jangan destructive git command kecuali user eksplisit.
- Jangan commit kecuali user eksplisit minta commit.
- Memory write wajib confirmation user.

## Reporting

Setelah mutating action:

- Print diff summary.
- Print verification result.
- Print rollback hint jika applicable.
````

===== END FILE =====

===== FILE: skills/context7.md =====

```markdown
# Skill: context7

description: MCP tool untuk library/framework docs lookup

## When to Use

Trigger Context7 saat:

- Question version-sensitive (API surface, deprecation, behavior change).
- Library unknown atau memory-uncertain.
- User mention specific library version.
- Code pakai unfamiliar import.

Do NOT use untuk:

- General language syntax (built-in).
- Stable, well-known APIs.
- User's own code.

## Flow

1. `resolve-library-id` dengan library name → get canonical ID.
2. `get-library-docs` dengan ID + optional topic → fetch docs.
3. Use docs sebagai primary source. Cite section.

## Fallback

- `resolve-library-id` returns nothing → report "library not in Context7", fallback web search if available.
- `get-library-docs` fails → report error, **do not fabricate API**.
- Docs too generic → narrow dengan `topic` parameter.

## Anti-Hallucination Rule

Never invent:

- function signature
- option name
- import path
- version number

Jika uncertain setelah Context7 → say "uncertain, please verify".
```

===== END FILE =====

===== FILE: skills/memory.md =====

````markdown
# Skill: memory

description: Memory policy & write protocol

## Trigger

- `/.memory` command.
- User says "ingat ini", "simpan ini", "remember this".
- Agent proposes via natural detection (rare).

## Qualifies as Long-Term Memory

- User identity / role / project context.
- Stable preference (tooling, style).
- Cross-session technical decision.
- Recurring constraint.

## Does NOT Qualify

- One-off task detail.
- Transient state.
- Anything time-bound ("hari ini …").
- Anything user might regret persisting.

## Proposal Format

```text
[MEMORY PROPOSAL]
Entry: <one-line fact>
Category: <identity | preference | project | constraint>
Scope: <permanent | until <event>>
Confirm? (y/n)
```

## Confirmation Rule

- No write without explicit `y` / `yes`.
- Ambiguous response (`mungkin`, `nanti`) → abort.
- User boleh revise sebelum confirm; loop sampai clean confirm atau abort.

## Forbidden

- Silent memory write.
- Memory write inferred dari "interesting" content.
- Memory of credentials / secrets / PII tanpa explicit override.
- Backdating atau rewriting past memory tanpa user request.
````

===== END FILE =====

===== FILE: skills/explore.md =====

````markdown
# Skill: explore

description: Codebase exploration — graphify-first, agent-workflow + fallback option

## Trigger

`/.explore <hint>`

## Pre-Flow Check

### STEP A — Multi-layer check AGENT_PATH

Jalankan L1–L5 check per `skills/agent-workflow.md`:

- **Semua layer PASS** → AGENT_PATH valid, lanjut STEP B.
- **Ada layer FAIL** → output error layer + tanya user:

  ```text
  [AGENT_PATH UNAVAILABLE]
  <error dari layer yang gagal>

  Lanjut /.explore tanpa AGENT_PATH (mode fallback: graphify-out + search/read lokal)? (yes/no)
  ```

  - User "no" → STOP.
  - User "yes" → lanjut ke **MODE FALLBACK**.

### STEP B — Cek graphify-out/ exists

```powershell
Test-Path -LiteralPath "<project_root>\graphify-out"
```

Jika hasil `False`, baru perlakukan graphify-out tidak ada. Jangan pakai `glob` sebagai satu-satunya bukti negatif.

- Ada → lanjut **PYTHON INVOKE** (STEP C).
- Tidak ada → STOP, suruh `graphify update`.

## STEP C — Python Invoke

### STEP C1 — Tentukan session

Resolve root project aktif.

- `MAIN_SESSION_PROJECT_ROOT` belum ada → generate baru.
- Ada dan normalized absolute path sama persis dengan root aktif → reuse `MAIN_SESSION_ID`.
- Path berbeda → generate `MAIN_SESSION_ID` baru.

### STEP C2 — Invoke python dengan caveman hint

```powershell
$promptFile = Join-Path $env:TEMP "opencode\agent_prompt.txt"
# isi file:
# <hint>
#
# [OUTPUT_STYLE]
# caveman ultra. Telegraphic. No filler. Scale output to complexity.
python $env:AGENT_PATH -c explore --prompt-file "$promptFile" -s "<session_id>" -w "<workspace_root>" --pretty
```

Tanpa inject `[WORKFLOW_AGENT]`. Tetap kirim `[OUTPUT_STYLE]` hint.

### STEP C3 — WAJIB tunggu JSON {ok, content, meta}

Parse JSON. **Proses BELUM SELESAI sampai `ok` ter-parse.**

- Output bukan JSON valid → STOP, tampilkan raw + error.
- `ok: false` → output `[EXPLORE FAILED]` dengan content, STOP.
- `ok: true` → lanjut STEP C4.

### STEP C4 — Output evidence + Set Cache

**WAJIB output hasil. Tidak boleh diam tanpa hasil.**

Tampilkan content (caveman-compressed dari workflow agent):

```text
[EXPLORE RESULT]
session: <session_id>
source:  agent-workflow (python)

<content dari JSON>

confidence: <dari meta>
```

**WAJIB**: Set `LAST_EXPLORE_RESULT` di context cache sesi untuk reuse oleh `/.plan` & `/.analyze`.

### STEP C5 — Token budget check

Jika `meta.token_usage` ada, akumulasi cumulative session. Trigger warn/gate sesuai threshold (50k/100k).

End with: `Lanjut /.plan atau /.analyze?`

## MODE FALLBACK — Tanpa AGENT_PATH

Caveman ultra **tetap aktif**. Graphify-out **wajib ada**.

### STEP F1 — Cek graphify-out/

```powershell
Test-Path -LiteralPath "<project_root>\graphify-out"
```

Tidak ada → STOP, suruh `graphify update`.

### STEP F2 — Eksplorasi sebagai agent utama

1. Baca `GRAPH_REPORT.md` + `graph.json` untuk struktur awal.
2. Identifikasi entry_points, related_modules, ownership.
3. Search/read file spesifik HANYA jika graph data tidak cukup.

### STEP F3 — Output evidence

**WAJIB output hasil. Tidak boleh diam tanpa hasil.**

```text
[EXPLORE RESULT]
session: <session_id>
source:  fallback (no AGENT_PATH) — graphify-out

entry_points:    <list seperlunya>
related_modules: <list seperlunya>
flow:            <summary grounded in evidence>
uncertainties:   <list jika memang ada>
```

**WAJIB**: Set `LAST_EXPLORE_RESULT` di context cache.

End with: `Lanjut /.plan atau /.analyze?`
````

===== END FILE =====

===== FILE: skills/plan.md =====

````markdown
# Skill: plan

description: Structured planning — evidence + reasoning layer dengan caveman compression

## Trigger

`/.plan <task>`

## Pre-Flow Check

### STEP A — Multi-layer check AGENT_PATH

Jalankan L1–L5 check. Ada FAIL → tanya fallback (yes/no).

### STEP B — Cek LAST_EXPLORE_RESULT di context

Ada → embed sebagai `[PRIOR_EVIDENCE]` di prompt python.
Tidak ada → prompt tetap tanpa prior evidence.

## STEP C — Python Invoke

### STEP C1 — Session

Reuse `MAIN_SESSION_ID` sesuai project root rule.

### STEP C2 — Invoke dengan prior evidence + caveman hint

```powershell
$promptFile = Join-Path $env:TEMP "opencode\agent_prompt.txt"
# isi file:
# <task>
#
# [PRIOR_EVIDENCE]
# <LAST_EXPLORE_RESULT content jika ada>
#
# [OUTPUT_STYLE]
# caveman ultra. Telegraphic. No filler. Scale output to complexity.
python $env:AGENT_PATH -c plan --prompt-file "$promptFile" -s "<session_id>" -w "<workspace_root>" --pretty
```

### STEP C3 — WAJIB tunggu JSON {ok, content, meta}

- `ok: false` → output `[PLAN FAILED]`, STOP.
- `ok: true` → lanjut STEP C4.

### STEP C4 — Synthesis plan final

**WAJIB output hasil. Tidak boleh diam.**

- Parse `content` workflow agent sebagai bahan evidence, bukan final output.
- Lakukan reasoning/synthesis sendiri.
- Turunkan `open_questions` dari evidence jika masih ada gap material untuk implementasi.

```text
[PLAN]
session: <session_id>
source:  opencode synthesis + agent-workflow evidence

scope:
- <ringkasan scope>

files_to_modify:
- <path + perubahan utama>

files_to_create:
- <path + tujuan>

steps:
1. <langkah implementasi>
2. <langkah implementasi>

execution_notes:
- <catatan penting dari evidence>

risks:
- <risk + dampak/mitigasi>

verification:
- <cara verifikasi>

open_questions:
- <pertanyaan turunan dari assumptions/implications/uncertainties jika masih ada gap material>

confidence:
  problem_understanding: <low | medium | high>
  solution_path: <low | medium | high>

uncertainties:
  - <hal yang memang belum bisa dikonfirmasi setelah evidence gathering>
```

Jika tidak ada gap material, tulis `open_questions: none`.

**WAJIB**: Set `LAST_PLAN_RESULT` di context cache.

### STEP C5 — Token budget check

Akumulasi cumulative. Trigger warn/gate jika perlu.

End with: `Lanjut /.execute -y?`

## MODE FALLBACK — Tanpa AGENT_PATH

Caveman ultra tetap aktif. Pakai `LAST_EXPLORE_RESULT` jika ada, atau lakukan exploration lite via graphify-out.

### STEP F1 — Cek graphify-out/

Sama seperti explore fallback.

### STEP F2 — Planning lokal

1. Baca `LAST_EXPLORE_RESULT` atau `GRAPH_REPORT.md`.
2. Identifikasi affected modules.
3. Draft plan langkah demi langkah.

### STEP F3 — Output plan (WAJIB output hasil)

Format sama dengan STEP C4, tapi `source: fallback synthesis`.

**WAJIB**: Set `LAST_PLAN_RESULT` di context cache.

End with: `Lanjut /.execute -y?`
````

===== END FILE =====

===== FILE: skills/execute.md =====

````markdown
# Skill: execute

description: Local implementation berdasarkan plan aktif

## Trigger

`/.execute -y` (tanpa `-y` → gate only).

Selalu lokal. Jangan cek atau require `AGENT_PATH`.

## Tanpa `-y`

Output `[EXECUTION SCOPE]` dan STOP. Tampilkan file yang akan disentuh + ringkasan perubahan.

## Dengan `-y`

### STEP 1 — Verify ada plan aktif

- `LAST_PLAN_RESULT` di context → pakai.
- Tidak ada → minta user run `/.plan` dulu, atau confirm untuk execute ad-hoc.

### STEP 2 — Permission gate untuk sensitive actions

Sesuai `skills/safety.md`. File mutation di scope normal tidak perlu per-file gate, tapi sensitive actions wajib.

### STEP 3 — Implement lokal

- Edit hanya file dalam execution scope.
- Caveman ultra aktif di output.
- Jangan modify file di luar scope.
- Jangan revert user changes.
- Jangan destructive git command kecuali user eksplisit.
- Jangan commit kecuali user eksplisit.

### STEP 4 — Output diff summary

```text
[EXECUTION RESULT]
session: <session_id>
files_changed: <count>
lines_added: <count>
lines_removed: <count>

Files:
  - <path>: <ringkasan perubahan>
```

**WAJIB**: Set `LAST_EXECUTE_DIFF` di context cache.

### STEP 5 — Auto-trigger verify selector

Run lightweight risk classifier setelah diff summary ada.

Tujuan hanya memilih verify command. Ini bukan full contract awareness, bukan pre-execute blocker, dan tidak boleh expand scope.

Low risk → `/.verify-quick`:

- 1-2 file.
- Localized copy/docs/style/simple bug fix.
- Tidak menyentuh risk signal.

Elevated risk → `/.verify`:

- 3+ file atau cross-module behavior.
- Config/env/dependency/build/test harness.
- Migration/schema/data mutation.
- Route/controller/public API/serialization surface.
- Auth/payment/security.
- Async/queue/scheduler/background job.
- Generated asset yang butuh build.

Jika ragu → `/.verify`.

`/.verify` dan `/.verify-quick` selalu lokal; no AGENT_PATH.

Jangan claim `done` sebelum verify selesai.
````

===== END FILE =====

===== FILE: skills/verify.md =====

````markdown
# Skill: verify

description: Full local verification setelah execute/refactor

## Trigger

`/.verify` (atau auto-triggered setelah `/.execute -y` untuk elevated-risk diff).

Selalu lokal. Jangan cek atau require `AGENT_PATH`.

## Steps

1. Run tests yang relevan (unit, integration jika ada).
2. Run linter/formatter.
3. Run build jika applicable.
4. Run type-check (tsc, mypy) jika applicable.
5. Spot-check perubahan terhadap plan aktif.

## Output

```text
[VERIFY RESULT]
status:   <pass | fail | warn>
checks:
  tests:    <count_pass>/<count_total> <PASS|FAIL>
  lint:     <PASS|FAIL>
  build:    <PASS|FAIL|SKIP>
  type:     <PASS|FAIL|SKIP>

<details jika ada fail>
```

## Rules

- Jangan claim `done` sebelum verify selesai.
- Verify tidak bisa dijalankan → jelaskan alasan.
- Verify fail → surface ke user, suggest fix.
- Runtime precondition belum ada → WARN/SKIP check terkait, bukan otomatis setup/config failure.
````

===== END FILE =====

===== FILE: skills/verify-quick.md =====

````markdown
# Skill: verify-quick

description: Lightweight local verify (syntax/lint only) untuk task kecil

## Trigger

`/.verify-quick` (atau auto-triggered untuk low-risk diff).

Selalu lokal. Jangan cek atau require `AGENT_PATH`.

## Steps

1. Syntax check (parse) untuk file yang diubah.
2. Lint untuk file yang diubah.
3. Type-check ringkas (jika applicable).

**Skip:** full test suite, build, integration test.

## Output

```text
[VERIFY-QUICK RESULT]
status:  <pass | fail>
checks:
  syntax: <PASS|FAIL>
  lint:   <PASS|FAIL>
  type:   <PASS|FAIL|SKIP>

<details jika fail>
```

## When NOT to Use

Gunakan lightweight risk classifier. Jika ada risk signal berikut, pakai `/.verify`:

- 3+ file atau cross-module behavior.
- Config/env/dependency/build/test harness.
- Migration/schema/data mutation.
- Route/controller/public API/serialization surface.
- Auth/payment/security.
- Async/queue/scheduler/background job.
- Generated asset yang butuh build.

Ini hanya selector verify, bukan full contract awareness.
````

===== END FILE =====

===== FILE: skills/refactor.md =====

```markdown
# Skill: refactor

description: Safe scoped refactor (plan + execute + verify sequence)

## Trigger

`/.refactor <scope>`

## Flow

1. Internally: `/.plan` untuk scope refactor (atau pakai `LAST_PLAN_RESULT` jika ada).
2. Permission gate sebelum execute.
3. `/.execute -y` dengan plan tersebut.
4. Auto-trigger `/.verify` (full, karena refactor biasanya architectural).
5. Optional: suggest `/.audit` untuk independent review.

## Constraint

- Scope wajib defined sebelum execute.
- Jangan auto-expand scope.
- Jangan modify file di luar refactor scope.

## Output

Sama dengan execute + verify, tapi prefix `[REFACTOR]` di status.
```

===== END FILE =====

===== FILE: skills/analyze.md =====

````markdown
# Skill: analyze

description: Deep analysis tanpa code changes, reuse LAST_EXPLORE_RESULT

## Trigger

`/.analyze <topic>`

## Pre-Flow

### STEP A — Multi-layer check AGENT_PATH

L1–L5. Ada FAIL → tanya fallback (yes/no).

### STEP B — Cek LAST_EXPLORE_RESULT

Ada → embed sebagai `[PRIOR_EVIDENCE]`.
Tidak ada → analyze tetap jalan, tapi mungkin perlu eksplorasi lite.

## STEP C — Python Invoke

Sama pattern dengan `/.plan`, beda command:

```powershell
python $env:AGENT_PATH -c analyze --prompt-file "$promptFile" -s "<session_id>" -w "<workspace_root>" --pretty
```

## STEP C4 — Output analysis (WAJIB output hasil)

- Workflow agent output boleh dipakai sebagai bahan utama.
- OpenCode agent utama tetap bertanggung jawab atas framing akhir ke user.
- Jika perlu, ringkas/rapikan hasil agar sesuai scope user. Jangan ubah fakta evidence.

```text
[ANALYSIS]
session: <session_id>
source:  opencode framing + agent-workflow evidence | fallback

summary:
- <inti analisis>

findings:
- <temuan utama>

implications:
- <dampak/arti temuan>

confidence: <low | medium | high>
uncertainties: <list>
```

Cache: tidak set ke `LAST_*` (analysis adalah read-only insight).

## MODE FALLBACK

Sama pattern dengan plan fallback. **WAJIB output hasil.**
````

===== END FILE =====

===== FILE: skills/audit.md =====

````markdown
# Skill: audit

description: Audit terhadap LAST_EXECUTE_DIFF. AGENT_PATH wajib. Cross-model preference (flexible).

## Trigger

`/.audit [scope]`

## Precondition

- `AGENT_PATH` valid (multi-layer L1-L5 check). Wajib; no fallback.
- `LAST_EXECUTE_DIFF` di context.

Tidak ada `LAST_EXECUTE_DIFF` → minta user execute dulu, atau pass diff manual via argument.

## Model Selection (Flexible)

**Preferensi**: invoke model berbeda dari executor untuk independent verdict.
**Tapi flexible** — jika model berbeda tidak available atau slow, model sama acceptable.

```powershell
# Preferred (cross-model):
python $env:AGENT_PATH -c audit --prompt-file "$promptFile" -s "<session_id>" -w "<workspace_root>" -m "moonshot/kimi-k2.6" --pretty

# Acceptable (same model):
python $env:AGENT_PATH -c audit --prompt-file "$promptFile" -s "<session_id>" -w "<workspace_root>" --pretty
```

Surface model used di output.

## Output Format

```text
[AUDIT]
session: <session_id>
model:   <model_name>
mode:    <cross-model | same-model>

findings:
  P0: <list — critical>
  P1: <list — high>
  P2: <list — medium>
  P3: <list — low>

recommendations: <list>
```

**WAJIB**: Set `LAST_AUDIT_RESULT` di context cache untuk re-fix loop.

## HARD STOP Rule

- AGENT_PATH unavailable → **HARD STOP**. Tidak ada fallback untuk audit (audit fallback ke same-model defeat the purpose).

Tapi jika user eksplisit force same-model audit dengan AGENT_PATH valid → acceptable.
````

===== END FILE =====

===== FILE: skills/init.md =====

````markdown
# Skill: init

description: Create project-local `.workflow` workspace for first-time workflow setup

## Trigger

`/.init`

## Behavior

- Resolve active project root.
- Do not bootstrap `.workflow` manually.
- Do not create ad-hoc minimal files.
- Invoke local runtime command only, using the active project root as `--work-dir`.
- Required invocation shape:

```powershell
python <resolved_runtime_path> -c init -w "<project_root>" --pretty
```

- Result source of truth is the runtime JSON response only.
- If runtime returns `ok: false`, surface it and stop.
- If target project already has valid `.workflow`, treat as idempotent success.
- If any required JSON file exists but invalid, stop and surface clear error.
- Ensure root `.gitignore` contains `.workflow/`.
- Do not create `.workflow/sessions/current.json`.

## Output

- Brief setup summary.
- Mention created files, existing files, and whether root `.gitignore` changed.
- If success, recommend `/.doctor` next.
- Do not claim success from reasoning alone; claim success only from runtime JSON.
````

===== END FILE =====

===== FILE: skills/doctor.md =====

````markdown
# Skill: doctor

description: Validate workflow project readiness using local runtime doctor command

## Trigger

`/.doctor`

## Behavior

- Resolve active project root.
- Do not assess readiness manually before runtime returns.
- Invoke local runtime command only, using the active project root as `--work-dir`.
- Required invocation shape:

```powershell
python <resolved_runtime_path> -c doctor -w "<project_root>" --pretty
```

- Result source of truth is the runtime JSON response only.
- If runtime returns `ok: false`, surface it and stop.
- Treat `graphify-out` missing as non-fatal.
- Summarize status as `READY` or `NOT_READY`.
- Surface issues and recommended fixes clearly.

## Output

- Status headline.
- Short list of issues.
- Short list of recommended fixes.
- Mention path to `.workflow/reports/doctor.json`.
- Never output `READY` or `NOT_READY` from agent reasoning alone.
````

===== END FILE =====

===== FILE: skills/sweep.md =====

````markdown
# Skill: sweep

description: Lightweight local post-edit impact scan using project git diff and workflow scope

## Trigger

`/.sweep`

## Behavior

- Resolve active project root.
- Do not inspect git diff manually as substitute for runtime sweep unless user explicitly asks for fallback analysis.
- Invoke local runtime command only, using the active project root as `--work-dir`.
- Required invocation shape:

```powershell
python <resolved_runtime_path> -c sweep -w "<project_root>" --pretty
```

- Result source of truth is the runtime JSON response only.
- If runtime returns `ok: false`, surface it and stop.
- If no diff, accept `skipped` verdict.
- If diff exists and no obvious issue, accept `pass` verdict.
- If risk indicators appear, surface `repair_required` verdict.
- Do not create separate repair command.

## Output

- Verdict.
- Reason.
- Changed files summary if relevant.
- Mention path to `.workflow/reports/sweep.last.md`.
````

===== END FILE =====

===== FILE: skills/help.md =====

````markdown
# Skill: help

description: Show command guide

## Trigger

`/.help`

## Output

```text
[OPENCODE WORKFLOW GUIDE V3.1.1]

Workflow (local utility):
  /.init                — bootstrap .workflow workspace in target project
  /.doctor              — validate workflow readiness locally
  /.sweep               — lightweight post-edit impact scan locally

Workflow (evidence):
  /.explore <hint>      — exploration, graphify-first
  /.plan <task>         — structured plan (reuse LAST_EXPLORE, synthesize final plan)
  /.analyze <topic>     — analysis (reuse LAST_EXPLORE, final framing by agent utama)

Workflow (action):
  /.execute -y          — local implementation dari plan aktif; no AGENT_PATH
  /.verify              — full local verification; no AGENT_PATH
  /.verify-quick        — lightweight local verify (syntax/lint); no AGENT_PATH
  /.refactor <scope>    — plan + execute sequence
  /.audit [scope]       — deferred / disabled in v3.1.1

Utility:
  /.memory <note>       — propose memory update
  /.help                — show this guide

Caveman family:
  /.commit              — caveman commit message
  /.review <file/PR>    — one-line per issue review
  /.compress <file>     — compress prose ke caveman-speak

Workflow patterns:
  First setup : /.init → /.doctor
  Large/risky : /.explore → /.plan → /.execute -y → /.sweep → /.verify
  Quick fix   : natural → /.execute -y → /.sweep → /.verify-quick
  Refactor    : /.refactor → auto verify

Natural prompts valid. Slash without /. prefix → INVALID COMMAND.
```
````

===== END FILE =====

===== FILE: commands/init.md =====

```markdown
---
description: Alias `/init` to the canonical workflow command `/.init`.
---

Execute the canonical workflow command below as if the user had typed it directly:

```text
/.init
```
```

===== END FILE =====

===== FILE: commands/doctor.md =====

```markdown
---
description: Alias `/doctor` to the canonical workflow command `/.doctor`.
---

Execute the canonical workflow command below as if the user had typed it directly:

```text
/.doctor
```
```

===== END FILE =====

===== FILE: commands/sweep.md =====

```markdown
---
description: Alias `/sweep` to the canonical workflow command `/.sweep`.
---

Execute the canonical workflow command below as if the user had typed it directly:

```text
/.sweep
```
```

===== END FILE =====

===== FILE: commands/explore.md =====

```markdown
---
description: Alias `/explore` to the canonical workflow command `/.explore`.
---

Execute the canonical workflow command below as if the user had typed it directly, preserving the user's arguments:

```text
/.explore <hint>
```
```

===== END FILE =====

===== FILE: commands/plan.md =====

```markdown
---
description: Alias `/plan` to the canonical workflow command `/.plan`.
---

Execute the canonical workflow command below as if the user had typed it directly, preserving the user's arguments:

```text
/.plan <task>
```
```

===== END FILE =====

===== FILE: commands/execute.md =====

```markdown
---
description: Alias `/execute` to the canonical workflow command `/.execute`.
---

Execute the canonical workflow command below as if the user had typed it directly, preserving the user's arguments:

```text
/.execute
```
```

===== END FILE =====

===== FILE: commands/verify.md =====

```markdown
---
description: Alias `/verify` to the canonical workflow command `/.verify`.
---

Execute the canonical workflow command below as if the user had typed it directly:

```text
/.verify
```
```

===== END FILE =====

===== FILE: commands/verify-quick.md =====

```markdown
---
description: Alias `/verify-quick` to the canonical workflow command `/.verify-quick`.
---

Execute the canonical workflow command below as if the user had typed it directly:

```text
/.verify-quick
```
```

===== END FILE =====

===== FILE: commands/refactor.md =====

```markdown
---
description: Alias `/refactor` to the canonical workflow command `/.refactor`.
---

Execute the canonical workflow command below as if the user had typed it directly, preserving the user's arguments:

```text
/.refactor <scope>
```
```

===== END FILE =====

===== FILE: commands/analyze.md =====

```markdown
---
description: Alias `/analyze` to the canonical workflow command `/.analyze`.
---

Execute the canonical workflow command below as if the user had typed it directly, preserving the user's arguments:

```text
/.analyze <topic>
```
```

===== END FILE =====

===== FILE: commands/audit.md =====

```markdown
---
description: Alias `/audit` to the canonical workflow command `/.audit`.
---

Execute the canonical workflow command below as if the user had typed it directly:

```text
/.audit
```

Note: `/.audit` deferred / disabled in v3.1.1.
```

===== END FILE =====

===== FILE: commands/memory.md =====

```markdown
---
description: Alias `/memory` to the canonical workflow command `/.memory`.
---

Execute the canonical workflow command below as if the user had typed it directly, preserving the user's arguments:

```text
/.memory <note>
```
```

===== END FILE =====

===== FILE: commands/help.md =====

```markdown
---
description: Alias `/help` to the canonical workflow command `/.help`.
---

Execute the canonical workflow command below as if the user had typed it directly:

```text
/.help
```
```

===== END FILE =====

===== FILE: commands/commit.md =====

```markdown
---
description: Caveman commit message (Conventional Commits, ≤50 char subject). Usage: /.commit
---

Read `~/.config/opencode/skills/caveman.md` section "Sub-Skills → /.commit" and follow it.
```

===== END FILE =====

===== FILE: commands/review.md =====

```markdown
---
description: One-line per issue review (🔴 P0, 🟡 P1, 🟢 P2, ⚪ P3). Usage: /.review <file or PR>
---

Read `~/.config/opencode/skills/caveman.md` section "Sub-Skills → /.review" and follow it.
```

===== END FILE =====

===== FILE: commands/compress.md =====

```markdown
---
description: Compress prose file ke caveman-speak. Backup .original.md. Usage: /.compress <filepath>
---

Read `~/.config/opencode/skills/caveman.md` section "Sub-Skills → /.compress" and follow it.
```

===== END FILE =====

===== FILE: reference/invocation-examples.md =====

````markdown
# Invocation Examples

Examples ini hanya untuk command yang memakai `AGENT_PATH`: `explore`, `plan`, `analyze`, `audit`.
`execute`, `verify`, dan `verify-quick` selalu lokal.

## PowerShell (Default — Windows native)

### Short prompt

```powershell
$response = & python $env:AGENT_PATH -c <agent_command> --prompt "<short prompt>" -s "<session_id>" -w "<work_dir>" --pretty
$parsed = $response | ConvertFrom-Json
if (-not $parsed.ok) { Write-Error $parsed.meta.error; return }
$parsed.content # explore/audit direct OK; plan/analyze need OpenCode post-processing
```

### Long prompt via file (Default untuk multi-line)

```powershell
$promptFile = Join-Path $env:TEMP "opencode\agent_prompt.txt"
New-Item -ItemType Directory -Force -Path (Split-Path $promptFile) | Out-Null

@"
<task>

[PRIOR_EVIDENCE]
<LAST_EXPLORE_RESULT content jika ada>

[OUTPUT_STYLE]
caveman ultra. Telegraphic. No filler. Scale output to complexity.
"@ | Set-Content $promptFile

$response = & python $env:AGENT_PATH -c <agent_command> --prompt-file "$promptFile" -s "<session_id>" -w "<work_dir>" --pretty
$parsed = $response | ConvertFrom-Json
if (-not $parsed.ok) { Write-Error $parsed.meta.error; return }
$parsed.content # explore/audit direct OK; plan/analyze need OpenCode post-processing
```

## Python subprocess (alternative)

```python
import os, subprocess, json
from pathlib import Path

script = os.environ.get("AGENT_PATH")
prompt_file = Path(os.environ.get("TEMP", ".")) / "opencode" / "agent_prompt.txt"
prompt_file.parent.mkdir(parents=True, exist_ok=True)
prompt_file.write_text(prompt, encoding="utf-8")

args = ["python", script, "-c", command, "--prompt-file", str(prompt_file),
        "-s", session, "-w", work_dir, "--pretty"]
result = subprocess.run(args, capture_output=True, text=True)

# WAJIB parse JSON dan cek field "ok"
data = json.loads(result.stdout)
if "ok" not in data:
    raise RuntimeError("missing ok field")
if not data["ok"]:
    raise RuntimeError(f"agent error: {data.get('meta', {}).get('error', 'unknown')}")
# explore/audit may surface content directly.
# plan/analyze require OpenCode post-processing before final user response.
print(data["content"])
```

## Ownership Notes By Command

- `explore` → evidence-first; `content` may be surfaced directly.
- `plan` → `content` is evidence/reasoning material; OpenCode agent must synthesize final structured plan.
- `analyze` → `content` is primary analysis material; OpenCode agent still owns final framing.
- `audit` → `content` may be surfaced directly as audit findings.

## Model Override

```powershell
python $env:AGENT_PATH -c plan --prompt-file "$promptFile" -s "..." -w "..." -m "anthropic/claude-sonnet-4-6" --pretty
```

```python
args += ["-m", "anthropic/claude-sonnet-4-6"]
```

## Cross-Model Audit (Preferred — Flexible)

```powershell
# Preferred cross-model:
python $env:AGENT_PATH -c audit --prompt-file "$promptFile" -s "..." -w "..." -m "moonshot/kimi-k2.6" --pretty

# Fallback same-model (acceptable jika cross-model unavailable):
python $env:AGENT_PATH -c audit --prompt-file "$promptFile" -s "..." -w "..." --pretty
```

## Common Command Examples

```powershell
python $env:AGENT_PATH -c explore --prompt-file "$promptFile" -s "main_finance_20260514_080000" -w "E:\Work\finance-v2" --pretty
python $env:AGENT_PATH -c analyze --prompt-file "$promptFile" -s "main_finance_20260514_080000" -w "E:\Work\finance-v2" --pretty
python $env:AGENT_PATH -c plan --prompt-file "$promptFile" -s "main_finance_20260514_080000" -w "E:\Work\finance-v2" --pretty
python $env:AGENT_PATH -c audit --prompt-file "$promptFile" -s "main_finance_20260514_080000" -w "E:\Work\finance-v2" --pretty
```
````

===== END FILE =====

===== FILE: reference/json-contract.md =====

````markdown
# JSON Contract (workflow agent response)

## Schema

```json
{
  "ok": true,
  "content": "string — primary response payload",
  "meta": {
    "model": "string — model name",
    "session_id": "string — workflow session id",
    "opencode_session_id": "string — MAIN_SESSION_ID",
    "confidence": "low | medium | high",
    "tokens_in": 1234,
    "tokens_out": 567,
    "duration_ms": 890,
    "error": "string — only present when ok:false"
  }
}
```

## Field Rules

- `ok` (bool, required) — operation success. **WAJIB ter-parse sebelum proses dianggap selesai.**
- `content` (string, required when ok:true) — main payload (evidence atau result atau error).
- `meta` (object, required) — telemetry + error.
- `meta.error` (string, required when ok:false) — human-readable reason.
- `meta.token_usage` (object, optional) — `{tokens_in, tokens_out}` untuk budget tracking.

## normalize_output()

Pseudocode:

```python
def normalize_output(raw):
    obj = json.loads(raw)
    assert "ok" in obj, "missing ok field"
    if obj["ok"]:
        assert "content" in obj, "missing content on ok:true"
    else:
        assert obj.get("meta", {}).get("error"), "missing error on ok:false"
    obj.setdefault("meta", {})
    return obj
```

## Valid Examples

Success:

```json
{
  "ok": true,
  "content": "Login flow uses session cookie...",
  "meta": {
    "model": "moonshot/kimi-k2.6",
    "session_id": "main_finance_20260514_080000",
    "confidence": "high",
    "tokens_in": 412,
    "tokens_out": 1180,
    "duration_ms": 3421
  }
}
```

Failure:

```json
{
  "ok": false,
  "content": "",
  "meta": {
    "model": "moonshot/kimi-k2.6",
    "error": "AGENT_PATH check failed at L3: not executable"
  }
}
```

## Invalid (reject)

- Missing `ok` → parse error.
- `ok: true` dengan empty `content` → parse error.
- `ok: false` tanpa `meta.error` → parse error.
- Non-string `content` → parse error.

## WAJIB: Wait For `ok` Flag (NON-NEGOTIABLE)

- Proses python invoke BELUM SELESAI sampai field `ok` ter-parse dari JSON output.
- Output bukan JSON valid → treat sebagai error, STOP. Jangan lanjut.
- `ok: false` → tampilkan `content`/`meta.error` sebagai error message, STOP.
- `ok: true` → lanjut ke post-processing sesuai command ownership.

## Command Ownership After Parse

- `explore` → `content` tidak boleh jadi final user response mentah; wajib disintesis jadi scoped exploration output oleh OpenCode agent sesuai hint user.
- `plan` → `content` tidak boleh jadi final user response mentah; wajib disintesis jadi structured plan oleh OpenCode agent.
- `analyze` → `content` boleh jadi bahan utama, tapi framing akhir tetap milik OpenCode agent.
- `audit` → `content` boleh diteruskan sebagai audit findings.
````

===== END FILE =====

===== FILE: reference/token-budget.md =====

````markdown
# Token Budget

## Thresholds

- **50k tokens cumulative** — soft warn, suggest `/.compress` atau scope trim.
- **100k tokens cumulative** — soft gate, require user confirm to continue.

## Counting

- Akumulasi `tokens_in + tokens_out` per `MAIN_SESSION_ID`.
- Reset counter on session reset atau project root change.
- Include tool result tokens jika tracked.

## Warn Format (50k)

```text
[TOKEN BUDGET WARN]
Session usage: <total>k token (threshold 50k tercapai).
Saran: pertimbangkan /.compress di file panjang, atau wrap up task aktif.
```

## Gate Format (100k)

```text
[TOKEN BUDGET GATE]
Session usage: <total>k token (threshold 100k).
Lanjut walau sudah 100k? (yes/no)
```

User "no" → STOP. User "yes" → lanjut, threshold berikutnya 150k (strong recommend reset session).

## Override

User: `"ignore token budget for this session"` → disable warn/gate untuk session itu.

## Notes

Threshold ini soft (warn + confirm, bukan hard stop). Tidak terlalu prominent di workflow normal — biasanya hanya kena di session panjang dengan banyak evidence commands.
````

===== END FILE =====

===== FILE: reference/graphify-missing-protocol.md =====

````markdown
# Graphify Missing Protocol

## Decision Tree

```
codebase task triggered
│
├─ Test-Path .\graphify-out
│  │
│  ├─ EXISTS
│  │  ├─ graph.json present?
│  │  │  ├─ YES → use as primary context
│  │  │  └─ NO  → treat as MISSING
│  │  └─ mtime > 7 days?
│  │     ├─ YES → warn, suggest `graphify update` (gate)
│  │     └─ NO  → proceed
│  │
│  └─ MISSING
│     ├─ task scope tiny (single file user-pointed)?
│     │  └─ YES → proceed without graph, note caveat
│     └─ task scope broad?
│        └─ surface to user:
```

```text
[GRAPHIFY MISSING]
Task needs codebase context.
graphify-out not found.
Options:
  1. Run `graphify update` (user) — recommended
  2. Narrow scope to specific files
  3. Proceed without graph (degraded accuracy)
Choose 1/2/3:
```

## Read-Only Mode Behavior

Jika user meminta workflow graphify-first dan `Test-Path` false dan mode read-only / plan-only:

1. **Jangan** buat file `.graphifyignore`.
2. Output template `.graphifyignore` yang disarankan sebagai teks.
3. Minta user menjalankan `graphify update` sendiri.
4. Jika task tetap harus lanjut tanpa graphify, lanjutkan hanya dengan search/read lokal dan tandai source sebagai `direct code inspection`.

## Write Mode Behavior

1. Buat `.graphifyignore` sesuai template.
2. Output `.graphifyignore` content + suruh user run `graphify update`.

## `.graphifyignore` Template

```text
# build artifacts
node_modules/
dist/
build/
.next/
.nuxt/

# python
__pycache__/
*.pyc
.venv/
venv/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# lockfiles
package-lock.json
yarn.lock
pnpm-lock.yaml
poetry.lock

# generated
*.min.js
*.bundle.js
coverage/

# project-specific (add as needed)
```

## Stale Graph Handling

- `graph.json` mtime > 7 days → warn.
- User confirms refresh → run `graphify update` (gate).
- User skips → proceed dengan caveat: "graph age: <n> days, results may miss recent changes".

## Framework Detection (jika perlu)

Detect dari sinyal minimal:

- `composer.json` / `artisan` → Laravel
- `requirements.txt` / `pyproject.toml` → Python
- `package.json` dengan NestJS → NestJS
- `package.json` dengan Next.js → Next.js
- `package.json` dengan React → React
- `Cargo.toml` → Rust
- `pubspec.yaml` → Flutter
````

===== END FILE =====

===== FILE: reference/errors.md =====

````markdown
# Error Messages Reference

## INVALID COMMAND

Trigger:

- slash command tanpa `/.` prefix (e.g. `/plan`)
- unknown `/.xxx`
- malformed argument

Format:

```text
[INVALID COMMAND]
Got: <input>
Reason: <missing /. prefix | unknown command | bad args>
Try: <closest valid command or list>
STOP.
```

## CHECK FAILED L1-L5

Trigger: AGENT_PATH precondition check fails.

Format:

```text
[CHECK FAILED — L<n>: <ENV NOT SET | FILE NOT FOUND | INVALID FILE TYPE | PYTHON NOT FOUND | SCRIPT NOT CALLABLE>]
Layer: <1-5>
Reason: <detail>
Action: <evidence command → tanya fallback | action command → HARD STOP>
```

## PERMISSION REQUIRED

Trigger: sensitive action tanpa prior gate.

Format: see `skills/safety.md` → Permission Gate Format.

## TOKEN BUDGET WARN

Trigger: 50k threshold crossed.
Format: see `reference/token-budget.md` → Warn Format.

## TOKEN BUDGET GATE

Trigger: 100k threshold crossed.
Format: see `reference/token-budget.md` → Gate Format.

## GRAPHIFY MISSING

Trigger: codebase task tanpa `graphify-out`.
Format: see `reference/graphify-missing-protocol.md`.

## CONTEXT7 UNAVAILABLE

Trigger: Context7 lookup needed but service down.

Format:

```text
[CONTEXT7 UNAVAILABLE]
Library: <name>
Fallback: <web search | user provide docs | proceed with uncertainty>
```

## SESSION ID MISMATCH

Trigger: invoke workflow tapi `MAIN_SESSION_PROJECT_ROOT` di context tidak match dengan project root aktif.

Format:

```text
[SESSION MISMATCH]
Context root:  <stored_path>
Current root:  <active_path>
Action: regenerating MAIN_SESSION_ID for current project root.
```

## EXPLORE/PLAN/ANALYZE FAILED

Trigger: workflow agent return `ok: false`.

Format:

```text
[<EXPLORE|PLAN|ANALYZE> FAILED]
session: <session_id>
error:   <meta.error>
STOP.
```

## OK FLAG MISSING

Trigger: JSON response tanpa `ok` field, atau bukan JSON valid.

Format:

```text
[INVALID RESPONSE]
Raw output: <truncated raw>
Reason: missing 'ok' field or not valid JSON
STOP.
```
````

===== END FILE =====

===== FILE: memory/PERSONAL_MEMORY.md =====

```markdown
# Personal Memory — OpenCode

Last updated: 2026-05-14

## Architecture Decisions

- (belum ada)

## Module Ownership

| Module | Team | Notes |
| ------ | ---- | ----- |
| -      | -    | -     |

## Known Landmines

- (belum ada)

## Things I Always Forget

- (belum ada)
```

===== END FILE =====

===== FILE: memory/DOMAIN_MAP.md =====

```markdown
# Domain Map — OpenCode

Last updated: 2026-05-14

## Entry Points

| Domain | Entry File | Key Function |
| ------ | ---------- | ------------ |
| -      | -          | -            |

## Cross-Team Boundaries

- (belum ada)
```

===== END FILE =====

===== FILE: memory/MEMORY.md =====

```markdown
# Memory Index

- [Personal Memory](PERSONAL_MEMORY.md) — architecture decisions, ownership, landmines
- [Domain Map](DOMAIN_MAP.md) — entry points, boundaries, domain notes
```

===== END FILE =====

---

## STEP VERIFY — Verifikasi file setup setelah file ditulis

Setelah semua file ditulis, verifikasi structural setup saja.

Hard fail setup hanya untuk:

- File/folder wajib tidak tertulis.
- Isi file generated tidak sesuai spec.
- Backup/restore logic gagal.

Jangan fail setup hanya karena runtime condition belum ada (contoh: sample project belum ada, `AGENT_PATH` belum di-set, graphify CLI belum installed, command smoke test belum bisa jalan). Runtime condition dicatat sebagai WARN/SKIP dan diuji di bagian Runtime Smoke Test.

1. Catat availability prerequisites dari STEP 0:
   - Caveman plugin: terinstall atau warning manual.
   - Graphify: `graphify --version` tersedia atau skipped/warn.
   Prerequisite unavailable = WARN/SKIP, bukan structural setup fail, selama file config yang di-generate benar.

2. List `~/.config/opencode/` dan subdirektori. Pastikan struktur sesuai.

3. Tampilkan ukuran byte file penting:
   - `AGENTS.md`
   - `install_guide.md`
   - Semua skill files (16 files)
   - Semua command files (13 files)
   - Semua reference files (5 files)
   - Memory index

4. Konfirmasi `AGENTS.md` mengandung:
   - `[BEHAVIOR LOCK]` section
   - `Session Handling Rule (WAJIB — Hard Rule)` dengan MAIN_SESSION_ID + project root
   - `Session Context Cache (Exploration Cache Contract)`
   - `Fallback Mode (KEEP — Resiliency)` section
   - `Global Forbidden` dengan "WAJIB output hasil" enforcement
   - `Command Registry`

5. Konfirmasi `skills/caveman.md`:
   - Hard rules section (no caps table — by design)
   - Inject Rule section
   - Sub-skills `/.commit`, `/.review`, `/.compress`

6. Konfirmasi `skills/audit.md`:
   - Cross-model preference note (flexible)
   - HARD STOP rule (no fallback)
   - `[AUDIT]` block format

7. Konfirmasi `skills/agent-workflow.md`:
   - L1-L5 multi-layer check
   - L1-L2 rule untuk invocation berikutnya
   - Session/Project Scoping HARD RULE
   - PowerShell-first invocation

8. Konfirmasi semua command files mengandung frontmatter `description`.

9. Konfirmasi `install_guide.md` mengandung Section 1-3 (Caveman / Graphify / Context7).

---

## POST-SETUP CHECKLIST (agent must print)

```text
[v3.1.1 FINAL SETUP STATUS]

Prerequisites:
  □ Caveman plugin                           OK / FAIL / SKIP
  □ Graphify CLI                             OK / FAIL / SKIP

Root files:
  □ AGENTS.md                                OK / FAIL
  □ install_guide.md                         OK / FAIL

Skills:
  □ skills/caveman.md                        OK / FAIL
  □ skills/workflow.md                       OK / FAIL
  □ skills/agent-workflow.md                 OK / FAIL
  □ skills/graphify.md                       OK / FAIL
  □ skills/safety.md                         OK / FAIL
  □ skills/context7.md                       OK / FAIL
  □ skills/memory.md                         OK / FAIL
  □ skills/explore.md                        OK / FAIL
  □ skills/plan.md                           OK / FAIL
  □ skills/execute.md                        OK / FAIL
  □ skills/verify.md                         OK / FAIL
  □ skills/verify-quick.md                   OK / FAIL
  □ skills/refactor.md                       OK / FAIL
  □ skills/analyze.md                        OK / FAIL
  □ skills/audit.md                          OK / FAIL
  □ skills/help.md                           OK / FAIL

Commands:
  □ commands/explore.md                      OK / FAIL
  □ commands/plan.md                         OK / FAIL
  □ commands/execute.md                      OK / FAIL
  □ commands/verify.md                       OK / FAIL
  □ commands/verify-quick.md                 OK / FAIL
  □ commands/refactor.md                     OK / FAIL
  □ commands/analyze.md                      OK / FAIL
  □ commands/audit.md                        OK / FAIL
  □ commands/memory.md                       OK / FAIL
  □ commands/help.md                         OK / FAIL
  □ commands/commit.md                       OK / FAIL
  □ commands/review.md                       OK / FAIL
  □ commands/compress.md                     OK / FAIL

Reference:
  □ reference/invocation-examples.md         OK / FAIL
  □ reference/json-contract.md               OK / FAIL
  □ reference/token-budget.md                OK / FAIL
  □ reference/graphify-missing-protocol.md   OK / FAIL
  □ reference/errors.md                      OK / FAIL

Memory:
  □ memory/PERSONAL_MEMORY.md                OK / FAIL / SKIP
  □ memory/DOMAIN_MAP.md                     OK / FAIL / SKIP
  □ memory/MEMORY.md                         OK / FAIL

Backups created: yes / no
Paths: <list of .bak files if applicable>

Runtime smoke: not run / run / skipped
Runtime smoke note: <WARN/SKIP reason jika ada>
```

---

## RUNTIME SMOKE TEST (terpisah dari setup verify)

Jalankan setelah structural setup pass.

Tujuan: validasi runtime behavior. Ini bukan syarat structural setup success.

Jika runtime precondition belum ada, mark test terkait `SKIP/WARN` dan tetap report. Jangan restore `.bak` hanya karena runtime condition belum tersedia.

Runtime preconditions:

- Ada project sample untuk command codebase.
- `AGENT_PATH` valid untuk evidence commands dan `/.audit`.
- Graphify tersedia/`graphify-out/` ada untuk evidence command pada codebase.
- Toolchain project tersedia untuk verify lokal.

Test ke-1 sampai ke-12 jika preconditions tersedia:

1. `cek logic login` → expect: caveman aktif + graphify check + WAJIB output hasil.
2. `/.explore login flow` → expect: L1-L5 check → graphify check → python invoke → JSON ok parse → output evidence + set LAST_EXPLORE_RESULT.
3. `/.plan tambah fitur X` → expect: structured plan + confidence + uncertainties + LAST_EXPLORE_RESULT pass sebagai PRIOR_EVIDENCE jika ada.
4. `/.execute -y` → expect: local implementation, no AGENT_PATH check, permission gate untuk sensitive actions, diff summary, auto-verify selector.
5. Low-risk diff → expect: lightweight risk classifier memilih `/.verify-quick`.
6. Elevated-risk diff → expect: lightweight risk classifier memilih `/.verify`.
7. `/.audit` → expect: deferred / disabled.
8. `READ-ONLY cek payment` → expect: evidence only, no mutation.
9. `normal mode` → expect: caveman OFF untuk session.
10. `stop caveman` → expect: caveman OFF untuk session.
11. `/plan` (no dot) → expect: `[INVALID COMMAND]`.
12. Pindah project root mid-session → expect: `MAIN_SESSION_ID` regenerate, cache reset.

Jika smoke fail karena generated file content salah → report which, restore `.bak`, do NOT claim runtime ready.
Jika smoke fail/skip karena runtime condition belum ada → report WARN/SKIP, jangan fail structural setup.

---

## FINAL

```text
[SETUP COMPLETE — OPENCODE GLOBAL WORKFLOW V3.1.1 FINAL]

Prerequisites:
  Caveman plugin → <installed — activate: /caveman ultra | FAILED — hard rules via skill>
  Graphify CLI   → <found <ver> | installed <ver> | skipped — install manually>

Config:
  ~/.config/opencode/AGENTS.md          ✓
  ~/.config/opencode/install_guide.md   ✓

Skills:    16 files ✓
Commands:  13 files ✓
Reference:  5 files ✓
Memory:     3 files ✓ (new | kept)

Total: 39 files

Status: READY

v3.1.1 FINAL Capabilities (parity dengan v3.0.1 yang teruji + bounded runtime update):
  Session isolation:    MAIN_SESSION_ID + project root scoping (HARD RULE)
  WAJIB output hasil:   no silent on /.explore /.plan /.analyze (HARD RULE)
  Fallback mode:        evidence commands tanpa AGENT_PATH (KEEP)
  Exploration cache:    LAST_EXPLORE_RESULT reused di /.plan & /.analyze
  Local bootstrap:      /.init /.doctor /.sweep aktif
  Resolver runtime:     .workflow/config.json dulu, fallback AGENT_PATH
  Session reset:        stale state reset saat --session berubah
  Auto sweep:           /.execute -y auto /.sweep
  Lightweight verify:   /.verify-quick (syntax/lint/type only)
  Verify selector:      lightweight risk classifier only, no full contract awareness
  Local execution:      /.execute -y, /.verify, /.verify-quick no AGENT_PATH
  Setup verify split:   structural setup != runtime smoke
  Caveman ultra:        hard rules + injection (no caps table)
  Token budget:         soft warn 50k, soft gate 100k

Removed (no empirical justification):
  Contract awareness pre-execute (deep downstream contract audit) — CUT
  Caveman hard caps per-section table — CUT (hard rules tetap)

Workflow patterns:
  First setup : /.init → /.doctor
  Large/risky : /.explore → /.plan → /.execute -y → /.sweep → /.verify
  Quick fix   : natural → /.execute -y → /.sweep → /.verify-quick
  Refactor    : /.refactor → auto verify

Active commands (workflow): /.init /.doctor /.sweep /.explore /.plan /.execute
                            /.verify /.verify-quick /.refactor /.analyze /.memory /.help
Active commands (caveman):  /.commit /.review /.compress
Invalid slash commands: /init /doctor /sweep /explore /plan /execute /verify /analyze — REJECTED
Natural prompts: VALID
Output default: CAVEMAN ULTRA (hard rules, scale to complexity, no caps table)
Graphify: PRIMARY SOURCE (wajib ada untuk evidence commands)
Context7: MCP DOCS TOOL (aktif untuk library/framework queries)
JSON contract (AGENT_PATH invocations): {ok, content, meta} — wajib tunggu parse `ok` sebelum proses selesai
[WORKFLOW_AGENT]: python-internal — OpenCode tidak inject
Fallback mode: evidence commands only (/.explore /.plan /.analyze), graphify-out wajib
Local commands: /.init /.doctor /.sweep /.execute -y /.verify /.verify-quick — no AGENT_PATH
Audit command: /.audit — deferred / disabled

🪨 WHY USE MANY TOKEN WHEN FEW DO TRICK
```
